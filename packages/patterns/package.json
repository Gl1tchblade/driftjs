{
  "name": "@driftjs/patterns",
  "version": "0.1.0",
  "description": "Migration safety patterns for DriftJS",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "check-types": "tsc --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "@driftjs/core": "workspace:*",
    "@driftjs/enhancer": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.3.0",
    "jest": "^29.7.0",
    "@types/jest": "^29.5.0",
    "ts-jest": "^29.1.0"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=18.0.0"
  }
} 